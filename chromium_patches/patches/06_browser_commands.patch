diff --git a/chrome/browser/ui/browser_commands.cc b/chrome/browser/ui/browser_commands.cc
--- a/chrome/browser/ui/browser_commands.cc
+++ b/chrome/browser/ui/browser_commands.cc
@@ -XXX,6 +XXX,10 @@
 #include "content/public/browser/navigation_controller.h"
 // ... other includes ...

+#if BUILDFLAG(IS_WIN)
+#include "chrome/browser/bladeblaid/profiles_lite_ui.h"
+#endif
+
 namespace chrome {
 // ... existing code ...

+#if BUILDFLAG(IS_WIN)
+void OpenBladeBlaidProfiles(Browser* browser) {
+  GURL profiles_url("chrome://profiles-lite");
+  NavigateParams params(browser, profiles_url, ui::PAGE_TRANSITION_TYPED);
+  params.disposition = WindowOpenDisposition::NEW_FOREGROUND_TAB;
+  Navigate(&params);
+}
+#endif
+
 }  // namespace chrome

diff --git a/chrome/browser/ui/browser_commands.h b/chrome/browser/ui/browser_commands.h
--- a/chrome/browser/ui/browser_commands.h
+++ b/chrome/browser/ui/browser_commands.h
@@ -XXX,6 +XXX,11 @@ void ShowAboutChrome(Browser* browser);
 void ShowHelp(Browser* browser, HelpSource source);
 // ... other declarations ...

+#if BUILDFLAG(IS_WIN)
+// Opens the BladeBlaid Profile Manager page
+void OpenBladeBlaidProfiles(Browser* browser);
+#endif
+
 }  // namespace chrome

 #endif  // CHROME_BROWSER_UI_BROWSER_COMMANDS_H_

