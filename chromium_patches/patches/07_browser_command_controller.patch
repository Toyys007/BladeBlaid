diff --git a/chrome/browser/ui/browser_command_controller.cc b/chrome/browser/ui/browser_command_controller.cc
--- a/chrome/browser/ui/browser_command_controller.cc
+++ b/chrome/browser/ui/browser_command_controller.cc
@@ -XXX,6 +XXX,10 @@
 #include "chrome/browser/ui/browser_commands.h"
 // ... other includes ...

+#if BUILDFLAG(IS_WIN)
+#include "chrome/app/chrome_command_ids.h"
+#endif
+
 namespace {
 // ... namespace content ...
 }  // namespace
@@ -XXX,6 +XXX,12 @@ void BrowserCommandController::InitCommandState() {
   command_updater_.UpdateCommandEnabled(IDC_OPTIONS, true);
   // ... other commands ...

+#if BUILDFLAG(IS_WIN)
+  // BladeBlaid Profiles command - always enabled
+  command_updater_.UpdateCommandEnabled(IDC_BLADEBLAID_PROFILES, true);
+#endif
+
 }

@@ -XXX,6 +XXX,13 @@ bool BrowserCommandController::ExecuteCommandWithDisposition(
     case IDC_OPTIONS:
       chrome::ShowSettings(browser_);
       break;
+
+#if BUILDFLAG(IS_WIN)
+    case IDC_BLADEBLAID_PROFILES:
+      chrome::OpenBladeBlaidProfiles(browser_);
+      break;
+#endif
+
     // ... other cases ...
     default:
       LOG(WARNING) << "Received unimplemented command: " << id;

